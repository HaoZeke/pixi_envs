name: elja_any_cpu
category: ''
query: '{}'
env:
  FW_CONFIG_FILE: /users/home/rog32/gpr-optim-fw/configs/FW_config.yaml
  # gpr_optim's gprd binary -- the GP-accelerated dimer driver.
  GPR_OPTIM_REPO: /users/home/rog32/gpr_optim
  GPR_OPTIM_GPRD: /users/home/rog32/gpr_optim/build-scalapack-real/gprd
  # Standalone eOn pixi env on elja already ships an eonclient binary.
  # The qadapter pre_rocket prepends EON_BIN/.. to PATH so the firetask's
  # shutil.which("eonclient") resolves without a wrapper.
  EON_BIN: /users/home/rog32/.pixi/envs/eon/bin/eonclient
  PETMAD_MODEL_DIR: /users/home/rog32/gpr-optim-fw/models
